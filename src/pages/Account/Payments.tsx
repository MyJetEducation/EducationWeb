import React from 'react';
import AccountSettingsContainer from '../../containers/AccountSettingsContainer';

const Payments = () => {
  return (
    <AccountSettingsContainer pageTitle="Payments">
      payments
    </AccountSettingsContainer>
  );
};

export default Payments;
