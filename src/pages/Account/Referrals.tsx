import React from 'react';
import AccountSettingsContainer from '../../containers/AccountSettingsContainer';

const Referrals = () => {
  return (
    <AccountSettingsContainer pageTitle="Referrals">
      referrals
    </AccountSettingsContainer>
  );
};

export default Referrals;
